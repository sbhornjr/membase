name: begin_and_commit_transaction
prompt: >
  Begin a transaction, set the key "x" to the value "1" in the
  "begin_and_commit_transaction" namespace, then commit the transaction and verify that
  the value was stored correctly.
expected_state:
  - namespace: begin_and_commit_transaction
    key: x
    expected_value: "1"